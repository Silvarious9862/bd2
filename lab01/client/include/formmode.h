#ifndef FORMMODE_H
#define FORMMODE_H

enum FormMode {
    ViewEdit,      // Просмотр с редактированием
    ViewReadOnly,  // Просмотр без редактирования
    Selection      // Выбор записи
};

#endif // FORMMODE_H
